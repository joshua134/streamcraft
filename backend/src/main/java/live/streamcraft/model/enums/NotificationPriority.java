package live.streamcraft.model.enums;

public enum NotificationPriority {
	LOW,        // Informational
    MEDIUM,     // Normal notification
    HIGH,       // Important (stream starting soon)
    URGENT      // Critical (account issue, payment failed)
}
